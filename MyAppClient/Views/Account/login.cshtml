@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css" />
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.4/css/all.min.css" />
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;700&display=swap" rel="stylesheet">
    <title>Login Page</title>
    <style>
        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(to right, #6a11cb, #2575fc);
            height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            margin: 0;
        }

        .login-container {
            background-color: #fff;
            padding: 30px;
            border-radius: 10px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            max-width: 400px;
            width: 100%;
        }

        .form-control {
            border-radius: 20px;
        }

        .btn-primary {
            border-radius: 20px;
            width: 100%;
        }

        h1 {
            font-weight: 700;
            color: #fff;
            text-align: center;
            margin-bottom: 40px;
        }

        .card-body {
            padding: 30px;
        }

        .text-danger {
            margin-bottom: 20px;
        }
    </style>
</head>
<body>
    <div class="container">
        <h1>Login</h1>
        <div class="row justify-content-center">
            <div class="login-container">
                <div class="card">
                    <div class="card-body">
                        @using (Html.BeginForm("Login", "Account", FormMethod.Post))
                        {
                            <form id="loginForm">
                                <div id="errorMessage" class="text-danger"></div>
                                <div class="form-group">
                                    <label for="username">Username</label>
                                    <input type="username" class="form-control" id="username" name="username" placeholder="Username" required />
                                </div>
                                <div class="form-group">
                                    <label for="password">Password</label>
                                    <input type="password" class="form-control" id="password" name="password" placeholder="Password" required />
                                </div>
                                <button type="submit" class="btn btn-primary"><i class="fas fa-sign-in-alt"></i> Login</button>
                            </form>
                            <div class="text-center mt-3">
                                <small>Don't have an account? <a href="@Url.Action("SignUp", "Account")" class="btn-link">Sign Up</a></small>
                            </div>
                        }
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- JavaScript dependencies (jQuery and Bootstrap Bundle with Popper.js included) -->
    <script src="https://code.jquery.com/jquery-3.5.1.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js"></script>

    <script>
        // $("#loginForm").submit(function (event) {
        //     event.preventDefault();

        //     // First AJAX call to authenticate and get the JWT token
        //     $.ajax({
        //         url: "http://myappapi:5211/api/v1/auth/login",
        //         type: "POST",
        //         contentType: "application/json",
        //         data: JSON.stringify({
        //             username: $("#email").val(),  // Sending username to get the token
        //             password: $("#password").val() // Sending password to authenticate
        //         }),
        //         success: function (response) {
        //             debugger
        //             // Store the JWT token
        //             var token = response.token;
        //             localStorage.setItem("jwtToken", token);

        //             // Second AJAX call to log in using the JWT token
        //             $.ajax({
        //                 url: "http://myappapi:5211/Home/Login",
        //                 type: "POST",
        //                 contentType: "application/json",
        //                 data: JSON.stringify({
        //                     token: token // Pass the JWT token as data
        //                 }),
        //                 success: function () {
        //                     // Redirect to the Index page
        //                     // window.location.href = "http://myappapi:5211/Home/Index";
        //                 },
        //                 error: function () {
        //                     console.error("Failed to authenticate with JWT token.");
        //                 }
        //             });
        //         },
        //         error: function () {
        //             $("#errorMessage").text("Invalid credentials.");
        //         }
        //     });
        // });


        /*
        localStorage.setItem("jwtToken", response.token);
        window.location.href = "https://localhost:7058/Home/Index";  // Use the correct base URL
        },
        error: function() {
            debugger
            $("#errorMessage").text("Invalid credentials"); // Display error message
        }
        });
        });
        */
    </script>
</body>
</html>
